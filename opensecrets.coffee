# Client for OpenSecrets.org API

request = require 'request'
fs = require 'fs'
moment = require 'moment'

ROOT_URL = 'http://www.opensecrets.org/api/'

API_KEY = process.argv[6]

class Client

module.exports = Client
